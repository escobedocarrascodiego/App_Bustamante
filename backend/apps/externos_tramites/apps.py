from django.apps import AppConfig


class ExternosTramitesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.externos_tramites"
    label = "externos_tramites"
    verbose_name = "Externos - Tramites (dbControl)"
